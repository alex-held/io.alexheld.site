version: "3.7"

services:
  frontend:
    env_file: .prod.env
    image: alexheld/io.alexheld.site:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HTTP_PORT}:80"
      - "${HTTPS_PORT}:443"
